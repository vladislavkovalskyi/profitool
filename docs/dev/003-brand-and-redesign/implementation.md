# 003 · Бренд и полная переделка дизайна — Implementation

- **Статус:** в работе, сделаны шаги 1, 2, 4 из 10
- **Обновлено:** 2026-09-21

## Сделано

| Шаг | Что вышло | Где |
|---|---|---|
| 1. Палитра | Токены из `DESIGN.md`: `signal #F18F37`, `signal-text`, `brand-a/b`, `bone #F2EFE9`, `bone-dim`, `bone-faint #8A8B87`, линии `hair` 0.10. Текст на `signal` везде чёрный (11 мест в 6 файлах, белый давал 2,4:1) | `globals.css`, `catalog/*`, `header`, `buy-box` |
| 2. Шрифты | Unbounded 600/900 на заголовки, Golos Text 400/500/600 на текст. Sofia Sans и Geologica удалены. Капс в `t-hero`…`t-h3` убран, размеры пересчитаны под широкий Unbounded. Базовый кегль 17px | `globals.css`, `[locale]/layout.tsx` |
| 4. Логотип | Компонент `Logo`: знак 44px и слово Unbounded 600 24px, на телефоне 34px и 19px. Стоит в шапке и подвале. Favicon (svg + png 32), `apple-touch-icon`, OG-картинка `og-base.png` | `layout/logo.tsx`, `header`, `footer`, `[locale]/layout.tsx` |

Контрасты замерены скриптом по формуле WCAG 2.x, все пары проходят AA. Значения в `DESIGN.md`
поправлены: первоначальные цифры были приблизительными.

## Что нашлось по дороге

- **Переполнение шапки на 390px.** Слово «PROFITOOL» в Unbounded шире, чем в Sofia Sans. Правый блок
  шапки вылезал на 23px. Новый лок-ап с меньшим кеглем на телефоне это закрыл: `scrollWidth` 390
  при окне 390.
- **`pnpm lint` даёт 11 ошибок `react-hooks/set-state-in-effect`.** Все от паттерна
  `useEffect(() => setMounted(true), [])`. На чистом коммите их тоже 11, эта работа их не добавила.
  Паттерн описан в `CLAUDE.md`, править отдельной задачей.
- **Старый `/icons/favicon.svg` остался в `public/`.** Ссылок на него больше нет, файл можно удалить.

## Не сделано

- Шаг 3: ~124 захардкоженных значения на токены, `--header-h` вместо шести `top-[92px]`.
- Шаги 5–9: главная, карточка товара, фильтры, корзина, чекаут, сравнение, страницы брендов и сервиса.
- Шапка пока старая по форме: высота 76px вместо 88, «Каталог» текстом, поиск не во всю ширину,
  язык капсом. Переделывается вместе с главной.

## Проверка

`npx tsc --noEmit` чисто. Главная снята на 1512 и 390px, лежит в `shots/step2-*` и `shots/step4-*`.
Битых картинок нет (скрипт проверяет `naturalWidth` у всех `<img>` после прокрутки).
`pnpm build` в этом заходе не гонялся.
